@page "/proximamente"
@inject NavigationManager Navigation
@rendermode InteractiveServer

<PageTitle>Módulo en Desarrollo - EnerfoneCRM</PageTitle>

<div class="container-fluid p-4">
    <div class="row justify-content-center align-items-center" style="min-height: 70vh;">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-lg border-0" style="border-radius: 15px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                <div class="card-body text-center text-white p-5">
                    <div class="mb-4">
                        <i class="bi bi-tools" style="font-size: 5rem; opacity: 0.9;"></i>
                    </div>
                    <h2 class="mb-4 fw-bold">Módulo en Desarrollo</h2>
                    <p class="lead mb-4" style="font-size: 1.1rem; line-height: 1.8;">
                        Estamos trabajando en esta funcionalidad para ofrecerle la mejor experiencia posible. 
                        Este módulo estará disponible próximamente.
                    </p>
                    <div class="alert alert-light text-dark mb-4" role="alert" style="border-radius: 10px;">
                        <i class="bi bi-info-circle-fill me-2"></i>
                        <strong>Nota:</strong> Nuestro equipo está desarrollando activamente esta característica 
                        para incorporarla en futuras actualizaciones del sistema.
                    </div>
                    <button @onclick="Volver" class="btn btn-light btn-lg px-5 py-3" style="border-radius: 10px; font-weight: 500; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);">
                        <i class="bi bi-arrow-left-circle me-2"></i>Volver al Inicio
                    </button>
                </div>
            </div>
            <div class="text-center mt-4 text-muted">
                <small>
                    <i class="bi bi-clock-history me-2"></i>
                    Gracias por su comprensión y paciencia
                </small>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        animation: fadeInUp 0.6s ease-out;
    }

    @@keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .btn-light:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15) !important;
        transition: all 0.3s ease;
    }
</style>

@code {
    private void Volver()
    {
        Navigation.NavigateTo("/home");
    }
}
